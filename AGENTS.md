# PartPilot 项目协作记忆

以下为用户于 2026-09-10 明确指定的项目约定，适用于本仓库后续任务。

## 模型分工与回显

- Astra 负责规划、分析、架构决策、争议处理、实质审查和最终验收。
- 普通任务与简单协调使用 Sol，推理强度 low。
- 有明确边界的非简单实现及对应验证使用 Luna，推理强度 xhigh。
- Luna xhigh 多次尝试仍无果时，先整理失败证据、原因和已尝试方案，再升级至 Terra high 执行；不要重复相同的确定性失败。
- 小任务不为满足分工而增加无价值的委派。委派时明确文件所有权、验收标准，并要求保留其他人的改动。
- 每次面向用户的进度、等待消息和最终回复都以实际输出模型标识开头，例如 `【Astra】`、`【Sol Low】`、`【Luna Xhigh】`、`【Terra High】`。
- 汇总其他模型的结论时，标注汇总者及来源；等待代理时同样保留模型前缀。
- 仅在运行时支持且实际使用相应模型/强度时使用该标识；不可伪称切换成功。模型不可用时明确说明限制和实际替代模型。
- 这些是开发协作约定，不代表产品内部已经实现多模型路由。

## 实现、审查、验证与提交

1. 先明确需求与验收条件，阅读相关调用链，复用现有实现，做最小可逆改动。
2. 每次功能实现后，必须以审查者视角检查本次完整改动：bug、遗漏边界条件、需求偏差和质量不足。
3. 修复确认有效的问题；修复后重新运行对应验证，再由 Astra 验收。不得把未验证的推测当作已修复问题。
4. 每个完成并通过审查、验证的功能都提交代码；仅暂存本任务文件，不混入用户无关改动。该约定授权本地提交，不授权 push、发布或部署。
5. 最终交付说明行为变化、验证结果、未解决限制及提交编号。失败或受阻的验证必须如实说明，不能将未完成工作称为已验收。
6. 仅要求分析、评审或提出改进方案时，不自动实现整套方案；明确要求记录项目记忆时，将约定写入本文件。
